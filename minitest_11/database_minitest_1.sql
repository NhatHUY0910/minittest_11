-- CREATE DATABASE minitest_1;

use minitest_1;

-- CREATE TABLE types (
--     id BIGINT AUTO_INCREMENT PRIMARY KEY,
--     name VARCHAR(255)
-- );

-- CREATE TABLE computers (
--     id BIGINT AUTO_INCREMENT PRIMARY KEY,
--     code VARCHAR(255),
--     name VARCHAR(255),
--     producer VARCHAR(255),
--     producer_id BIGINT,
--     FOREIGN KEY (producer_id) REFERENCES types(id)
-- );

